interface:
  display_name: "Netron"
  short_description: "Repo-specific patterns and workflows for netron"
  default_prompt: "Use the netron repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true